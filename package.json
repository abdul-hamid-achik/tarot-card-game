{
  "name": "tarot-card-game",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm run -w @tarot/game-sim build && npm run -w @tarot/db build && npm run -w @tarot/ws-stub build && npm run -w @tarot/web build && npm run -w @tarot/docs build",
    "test": "npm run -w @tarot/game-sim test && echo 'Run web E2E locally with: npm run -w @tarot/web dev & npx -w @tarot/web playwright test'",
    "dev:web": "npm run -w @tarot/web dev",
    "docs": "npm run -w @tarot/docs dev",
    "test:watch": "npm run -w @tarot/game-sim test:watch",
    "lint": "eslint . --ext .ts,.tsx --max-warnings=0",
    "format": "prettier --write .",
    "e2e:web": "npx -w @tarot/web playwright test",
    "export:godot": "godot --headless --path clients/godot/project --export-release Web apps/web/public/godot/index.html",
    "export:godot:clean": "rm -rf apps/web/public/godot/*",
    "export:godot:dev": "npm run export:godot:clean && npm run export:godot && npm run dev:web"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.6.0",
    "@typescript-eslint/parser": "^7.6.0",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-import": "^2.29.1",
    "prettier": "^3.2.5"
  }
}