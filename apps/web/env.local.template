# Local development with neon_local Docker container
# The connection string will be shown in the neon_local logs when you start the container
DATABASE_URL=postgresql://neondb_owner@localhost:55432/neondb

# For production with Neon cloud database, use your actual Neon connection string:
# DATABASE_URL=postgresql://username:password@your-project.us-east-1.aws.neon.tech/neondb?sslmode=require

# Optional: Vercel Postgres URL (if using Vercel Postgres instead of Neon)
# POSTGRES_URL=your_vercel_postgres_url
